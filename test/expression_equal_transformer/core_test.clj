(ns expression-equal-transformer.core-test
  (:require [clojure.test :refer :all]
            [expression-equal-transformer.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
